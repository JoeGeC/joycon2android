package com.joegec.joycon2android.buttonmapping

/**
 * Layouts of one game in different grips. Setting one of a family sets every player to whichever of
 * them their own body can be held in, since a table is rarely holding the same thing.
 */
enum class LayoutFamily { MARIO_KART }

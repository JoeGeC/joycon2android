package com.joegec.joycon2android.buttonmapping.target

/** A GameCube controller's two analog sticks, only routable when a full body is connected. */
enum class GameCubeStick(val displayName: String) {
    MainStick("Main Stick"),
    CStick("C-Stick"),
}

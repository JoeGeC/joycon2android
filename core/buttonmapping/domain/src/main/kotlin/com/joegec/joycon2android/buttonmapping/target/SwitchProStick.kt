package com.joegec.joycon2android.buttonmapping.target

/** A Pro Controller's two analog sticks, only routable when a full body is connected. */
enum class SwitchProStick(val displayName: String) {
    LStick("L-Stick"),
    RStick("R-Stick"),
}
